<main>
  <div class="container py-4">

    <%= render "partials/header" %>
    <div class="p-5 mb-4 bg-light rounded-3">
      <p class="col-md-8 fs-4"><%= flash[:notice] %></p>
    </div>
    <%= render "partials/footer"%>

  </div>
</main>